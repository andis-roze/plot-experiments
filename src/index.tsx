import * as React from "react";
import * as ReactDOM from "react-dom";

const app = document.getElementById("root");

if (app) {
    ReactDOM.render(
        (
            <div />
        ),
        app
    );
}
